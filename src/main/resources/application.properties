spring.application.name=backend
server.port=8080

# ==========================================
# ☁️ SUPABASE CLOUD DATABASE (Active)
# ==========================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.profiles.active=dev

# Automatically creates tables in the cloud
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Increase maximum upload size for RoadWise image reports
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ==========================================
# 📧 SPRING MAIL CONFIGURATION (SMTP)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# spring.mail.username=csjdmroadwise@gmail.com
# spring.mail.password=xhix syzq jhxy phyz

# spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
# spring.datasource.username=postgres.elojstmlgvgpvdtitqib
# spring.datasource.password=Guella@072524

# ==========================================
# 💻 LOCAL PGADMIN DATABASE (Offline Backup)
# ==========================================
# spring.datasource.url=jdbc:postgresql://localhost:5432/roadwise_db
# spring.datasource.username=postgres
# spring.datasource.password=admin